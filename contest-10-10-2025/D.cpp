#include <bits/stdc++.h>
using namespace std;

#define fast_io ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);
#define endl "\n"

typedef long long ll;

const ll INF = 1e18;



int main(){ fast_io
    string pal;
    cin >> pal;

    int start = 0, 
        end = (int) pal.length();


    int l = 0, r = end;
    while (l <= r) {
        
    }

    cout << pal.substr(start, end - start) << endl;

    return 0;
}